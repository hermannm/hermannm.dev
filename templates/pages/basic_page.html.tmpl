<!doctype html>
<html lang="en-US">
  {{- template "head.html.tmpl" .Meta -}}
  <body
    class="mx-auto mb-4 mt-4 flex min-h-[var(--page-height)] max-w-2xl flex-col gap-4 bg-gruvbox-bg0 px-2 leading-[normal] text-gruvbox-fg"
  >
    <h1 class="flex justify-center text-2xl font-bold">
      <a href="/">{{ .Meta.Common.SiteName }}</a>
    </h1>

    {{ .Content }}

    {{ template "footer.html.tmpl" .Meta.Common }}
  </body>
</html>
